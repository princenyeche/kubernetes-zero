#!/bin/bash
# short cut keys on terminal
shopt -s expand_aliases
alias k=kubectl
alias t=task
alias tl='task --list-all'
alias mk="minikube kubectl --"
# accessing bash_profile
# cd ~/
# if none exist create one